<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dictionary</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400..700&family=Sacramento&display=swap" rel="stylesheet">
    <link href="styles/style.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1><a href="/">Dictionary</a></h1>
        <h2>Search a word in English!</h2>
        <form action="/search" id="form" class="form" method="POST">
            <!-- <label for="word">First name:</label><br> -->
            <input type="text" id="input" name="word" placeholder="Type your word here" autofocus autocomplete="off">
            <button type="submit"><svg id="icon" xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0"/>
            </svg></button>
        </form>
        <% if (locals.word) { %>
            <div class="sub-container">
                    <h3><%= word %></h3>
                    <% if (locals.content[0].hwi.prs){ %>
                    <p id="pronunciation">/<%= content[0].hwi.prs[0].mw %>/</p>
                    <%}%>
                    <h4>Definition:</h4>
                    <% content.forEach((item) =>{ %>
                    <div id="def">
                        <span id="item"> ‣ <%= item.meta.id %> </span> <br><span id="shortdef"><%= item.shortdef %></span>
                    </div>
                    <%});%>   
            </div>
        <%} else if (locals.trigger) {%>
            <h3><%= message %></h3>
        <%}%>
    </div>
<%- include("partials/footer.ejs") %>
</body>
</html>
